name: pytorch_env
channels:
  - pytorch
  - conda-forge # 依存関係解決のためconda-forgeを優先
  - defaults
dependencies:
  - python
  - pytorch
  - torchvision
  - torchaudio
  # CPU版ではpytorch-cudaを指定しない
  - numpy
