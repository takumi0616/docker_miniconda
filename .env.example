# .env.example
# このファイルを .env にコピーして自分用に編集してください。
# 例) cp .env.example .env

# 通知の送信先 ntfy サーバ
# 公開の無料サーバ（手軽に使える、課金不要）
NTFY_SERVER=https://ntfy.sh

# 通知のトピック（必ず自分で長いランダム文字列に変更してください）
# iOS/Android/Mac ではこのトピックを購読（Follow/Subscribe）して通知を受け取ります。
NTFY_TOPIC=takumin0616-docker-miniconda

# 自前 ntfy サーバを使う場合のみ。ntfy.sh では通常不要です。
# NTFY_TOKEN=

# タイムゾーン（通知メッセージの時刻・ログに表示されることがあります）
TZ=Asia/Tokyo